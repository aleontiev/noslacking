# CLAUDE.md

## Running commands

Always use `uv run` to execute Python commands in this project (e.g. `uv run noslacking status`). Do not use `python`, `python3`, or `pip` directly — pyenv is configured but the Python version may not be installed. `uv` handles the virtual environment and dependencies automatically.
